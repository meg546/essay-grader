---
phase: 07-data-contracts-route-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/types.ts
  - src/api/mock-data.ts
  - src/api/grading.ts
autonomous: true
requirements:
  - HLGT-01

must_haves:
  truths:
    - "Mock API grading responses include highlight ranges (start, end, categoryId) for every feedback category"
    - "Each CategoryScore has an id field for reliable cross-referencing"
    - "GradingResult includes the full essay text (not just excerpt) for downstream highlighting"
    - "Each highlight has a type field distinguishing strengths from improvements"
  artifacts:
    - path: "src/api/types.ts"
      provides: "HighlightRange interface, updated CategoryScore with id + highlights, updated GradingResult with essayText"
      contains: "HighlightRange"
    - path: "src/api/mock-data.ts"
      provides: "Full mock essay text and highlight ranges with valid character offsets"
      contains: "mockEssayText"
    - path: "src/api/grading.ts"
      provides: "gradeEssay passes essayText through to result"
      contains: "essayText"
  key_links:
    - from: "src/api/mock-data.ts"
      to: "src/api/types.ts"
      via: "import HighlightRange, CategoryScore"
      pattern: "HighlightRange"
    - from: "src/api/grading.ts"
      to: "src/api/types.ts"
      via: "GradingResult with essayText field"
      pattern: "essayText.*request\\.essayText"
---

<objective>
Add highlight range data contract and mock data to the API layer.

Purpose: Phase 9 needs highlight ranges to render color-coded essay passages. The data shape must be defined now so downstream phases build against a stable contract. Also adds `essayText` to `GradingResult` (research open question 1) and `id` to `CategoryScore` (research open question 2) to prevent breaking changes later.

Output: Updated type definitions, mock data with realistic highlight offsets, grading function passing essay text through.
</objective>

<execution_context>
@/Users/matthewgardner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewgardner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-contracts-route-restructure/07-CONTEXT.md
@.planning/phases/07-data-contracts-route-restructure/07-RESEARCH.md

@src/api/types.ts
@src/api/mock-data.ts
@src/api/grading.ts
@src/stores/app-store.ts

<interfaces>
<!-- Current types that will be modified -->
From src/api/types.ts:
```typescript
export interface CategoryScore {
  name: string;
  score: number;
  maxScore: number;
  strengths: string[];
  improvements: string[];
  justification: string;
}

export interface GradingResult {
  id: string;
  essayExcerpt: string;
  overallScore: number;
  maxScore: number;
  summary: string;
  categories: CategoryScore[];
  gradedAt: string;
}
```

From src/api/grading.ts:
```typescript
export async function gradeEssay(request: GradeEssayRequest): Promise<GradingResult>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define highlight data contract in types.ts</name>
  <files>src/api/types.ts</files>
  <action>
Add `HighlightRange` interface to `src/api/types.ts` with these fields (per user decision):
- `start: number` -- character offset into raw essay text
- `end: number` -- character offset (exclusive)
- `categoryId: string` -- links to parent rubric category
- `type: 'strength' | 'improvement'`

Update `CategoryScore` to add:
- `id: string` -- kebab-case identifier (e.g., "content-ideas", "organization") for reliable lookups in Phase 9. Keep `name` for display.
- `highlights: HighlightRange[]` -- one highlight per strength/improvement feedback point

Update `GradingResult` to add:
- `essayText: string` -- full essay text for rendering with highlights in Phase 9 (keeps `essayExcerpt` for backward compatibility with history display)

Do NOT move highlights to GradingResult top-level (per research anti-pattern). Keep them co-located on CategoryScore.
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>HighlightRange interface exported, CategoryScore has id + highlights fields, GradingResult has essayText field, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add mock essay text and highlight data to mock-data.ts, update grading.ts</name>
  <files>src/api/mock-data.ts, src/api/grading.ts</files>
  <action>
**In src/api/mock-data.ts:**

1. Add a `mockEssayText` export -- a multi-paragraph mock essay (~500-800 chars) about technology in education (matching the existing excerpt theme). The existing `essayExcerpt` should be the first ~120 chars of this text.

2. Add `id` field to each CategoryScore constant:
   - contentAndIdeas: `id: "content-ideas"`
   - organization: `id: "organization"`
   - styleAndVoice: `id: "style-voice"`
   - languageConventions: `id: "language-conventions"`

3. Add `highlights` array to each CategoryScore. Each strength and improvement should get one highlight. Use `mockEssayText.indexOf("target phrase")` to compute offsets programmatically, or manually verify with comments showing `mockEssayText.slice(start, end)`. The highlights array length should equal `strengths.length + improvements.length` for each category (one highlight per feedback point).

   Example for contentAndIdeas:
   ```typescript
   highlights: [
     { start: X, end: Y, categoryId: "content-ideas", type: "strength" },
     // ... one per strength and improvement
   ]
   ```

4. Add `essayText: mockEssayText` to the `mockGradingResult` object.

5. Update `getMockGradingResultById` to also spread `essayText`.

**In src/api/grading.ts:**

Update the `gradeEssay` function to include `essayText: request.essayText` in the returned object, so the full text submitted by the user is available in the result for highlighting.

**Important:** Overlapping highlights across categories are allowed (per user decision). Ensure at least one pair of highlights from different categories overlaps to validate Phase 9 handles this correctly.

**Pitfall avoidance:** Ensure highlight offsets do not exceed `mockEssayText.length`. Verify each `start < end` and both are within bounds.
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>mockEssayText exported with multi-paragraph essay, all four CategoryScore objects have id and highlights fields, mockGradingResult includes essayText, gradeEssay passes request.essayText through, build succeeds with no errors</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (type safety)
- `npm run build` succeeds (no build errors)
- HighlightRange is exported from types.ts
- Each CategoryScore in mock-data has highlights array with length equal to strengths.length + improvements.length
- All highlight offsets are valid (start < end, end <= mockEssayText.length)
- gradeEssay includes essayText in returned result
</verification>

<success_criteria>
- Mock API grading responses include highlight ranges with valid character offsets for every feedback category
- Each highlight has start, end, categoryId, and type fields
- At least one overlapping highlight pair exists across categories
- GradingResult includes full essayText alongside existing essayExcerpt
- TypeScript compiles and build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-contracts-route-restructure/07-01-SUMMARY.md`
</output>
