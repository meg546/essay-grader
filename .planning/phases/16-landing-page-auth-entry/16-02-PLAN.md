---
phase: 16-landing-page-auth-entry
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/pages/LandingPage.tsx
  - src/components/landing/HeroSection.tsx
  - src/components/landing/FeatureHighlights.tsx
  - src/components/landing/HowItWorks.tsx
  - src/components/landing/WalkthroughDemo.tsx
  - src/components/landing/Footer.tsx
autonomous: false
requirements:
  - LAND-01
  - LAND-02

must_haves:
  truths:
    - "User sees a hero section with headline, subtitle, and Sign In + Register CTA buttons"
    - "User sees feature highlight cards describing the product capabilities"
    - "User sees a how-it-works section with 3 steps (paste essay, add rubric, get feedback)"
    - "User sees an animated walkthrough demo showing the grading flow"
    - "User sees a footer with app name and copyright"
    - "Sections animate in on scroll with subtle motion effects"
    - "Page looks correct in both light and dark mode"
  artifacts:
    - path: "src/components/landing/HeroSection.tsx"
      provides: "Hero with headline, subtitle, CTA buttons"
      exports: ["HeroSection"]
    - path: "src/components/landing/FeatureHighlights.tsx"
      provides: "Feature cards grid with icons and descriptions"
      exports: ["FeatureHighlights"]
    - path: "src/components/landing/HowItWorks.tsx"
      provides: "3-step how-it-works section"
      exports: ["HowItWorks"]
    - path: "src/components/landing/WalkthroughDemo.tsx"
      provides: "Auto-playing animated demo of grading flow"
      exports: ["WalkthroughDemo"]
    - path: "src/components/landing/Footer.tsx"
      provides: "Landing page footer"
      exports: ["Footer"]
  key_links:
    - from: "src/pages/LandingPage.tsx"
      to: "src/components/landing/HeroSection.tsx"
      via: "import and render, passes onSignIn and onRegister callbacks"
      pattern: "HeroSection"
    - from: "src/components/landing/HeroSection.tsx"
      to: "src/components/ui/button.tsx"
      via: "Button components for CTAs"
      pattern: "Button.*Sign In|Register"
    - from: "src/components/landing/FeatureHighlights.tsx"
      to: "motion/react"
      via: "whileInView staggered animation"
      pattern: "whileInView"
---

<objective>
Build the full landing page content: hero section with CTAs, feature highlights, how-it-works steps, animated walkthrough demo, and footer. All sections use scroll-triggered animations via motion/react.

Purpose: Communicates the product value to unauthenticated users and gives them clear paths to sign in or register (LAND-01, LAND-02).
Output: A complete, polished landing page with all five content sections.
</objective>

<execution_context>
@/Users/matthewgardner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewgardner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-landing-page-auth-entry/16-CONTEXT.md
@.planning/phases/16-landing-page-auth-entry/16-RESEARCH.md
@.planning/phases/16-landing-page-auth-entry/16-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/pages/LandingPage.tsx (Plan 01 shell):
```typescript
// LandingPage manages: showSignIn state, handleAuthenticated callback, auth redirect
// Plan 02 replaces the placeholder sections with real components
// SignInDialog is already wired -- onSignIn triggers setShowSignIn(true)
// onRegister triggers navigate("/register")
```

From src/components/ui/button.tsx:
```typescript
// Button variants: "default" | "destructive" | "outline" | "secondary" | "ghost" | "link"
// Sizes: "default" | "sm" | "lg" | "icon"
```

From src/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent }
```

From motion/react (already installed):
```typescript
import { motion } from "motion/react"
// Key APIs: whileInView, viewport={{ once: true }}, staggerChildren, variants
```

Tailwind theme tokens (from src/index.css):
- bg-background, text-foreground (page base)
- bg-card, text-card-foreground (card surfaces)
- bg-primary, text-primary-foreground (brand color)
- text-muted-foreground (secondary text)
- border-border (borders)
- bg-accent, text-accent-foreground (hover states)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build landing page section components (Hero, Features, HowItWorks, Walkthrough, Footer)</name>
  <files>src/components/landing/HeroSection.tsx, src/components/landing/FeatureHighlights.tsx, src/components/landing/HowItWorks.tsx, src/components/landing/WalkthroughDemo.tsx, src/components/landing/Footer.tsx</files>
  <action>
Create all five landing page section components in `src/components/landing/`. Use only existing Tailwind theme tokens (bg-background, text-foreground, bg-card, text-muted-foreground, bg-primary, text-primary-foreground, border-border). Use Lucide icons only. Use motion/react for scroll-triggered animations.

1. **HeroSection.tsx** -- Props: `{ onSignIn: () => void; onRegister: () => void }`
   - Large headline: "Get Instant, Rubric-Aligned Essay Feedback" (or similar compelling copy)
   - Subtitle: brief description of what the app does
   - Two CTA buttons: "Sign In" (Button default/primary, size="lg", calls onSignIn) and "Register" (Button variant="outline", size="lg", calls onRegister)
   - Use motion.div for a fade-in entrance animation (initial opacity 0/y 20, animate to visible)
   - Generous vertical padding (py-24 md:py-32), max-w-3xl mx-auto, text-center
   - GraduationCapIcon as a large decorative element above the headline

2. **FeatureHighlights.tsx** -- No props needed
   - Grid of 4 feature cards (sm:grid-cols-2 lg:grid-cols-4, gap-6)
   - Features: Instant Feedback (Zap icon), Rubric-Aligned (Target icon), Highlighted Passages (Sparkles icon), PDF Support (FileText icon)
   - Each card: Card component with icon, title (font-semibold), description (text-sm text-muted-foreground)
   - Use motion variants with staggerChildren (0.1s) on the container, whileInView="visible" with viewport={{ once: true, margin: "-80px" }}
   - Child cards: variants hidden={opacity:0, y:20} visible={opacity:1, y:0, transition duration 0.5}
   - Section wrapper: max-w-6xl mx-auto px-4 py-16, centered section heading "Why EssayGrader?" above the grid

3. **HowItWorks.tsx** -- No props needed
   - 3 steps in a horizontal row (md:flex-row, flex-col on mobile)
   - Each step: numbered circle (bg-primary text-primary-foreground, size-12, rounded-full, flex items-center justify-center, text-lg font-bold) + title + description
   - Steps: "1. Paste Your Essay", "2. Add a Rubric", "3. Get Feedback" with brief descriptions
   - Connecting lines between steps on desktop (hidden on mobile): a `div` with `h-px w-12 bg-border`
   - Section wrapper: max-w-4xl mx-auto px-4 py-16, section heading "How It Works"
   - Scroll-triggered animation: wrap in motion.section with whileInView fade-in

4. **WalkthroughDemo.tsx** -- No props needed, self-contained animated demo
   - A Card component with overflow-hidden containing a mock UI simulation
   - Auto-playing loop showing 4 sequential frames with delay-based sequencing:
     Frame 1 (0-2s): Mock essay text appearing with a typing-like animation (reveal lines of sample text progressively using motion.div with staggered opacity)
     Frame 2 (2-3.5s): A rubric card/badge fading in beside the essay
     Frame 3 (3.5-5s): A loading/grading indicator (spinner or progress bar)
     Frame 4 (5-8s): Mock results appearing -- score bars (colored divs at different widths), a mock highlighted text snippet
   - After frame 4, pause briefly then loop (use motion animate with repeat/repeatType)
   - Use only CSS transforms and opacity for performance. Hardcoded sample text and mock scores -- NO real API calls
   - Trigger animation only when in viewport (whileInView) to avoid unnecessary rendering
   - Section wrapper: max-w-4xl mx-auto px-4 py-16, section heading "See It In Action"
   - Keep it visually compelling but implementation-simple -- this is a marketing animation, not a real interactive demo

5. **Footer.tsx** -- No props needed
   - Simple footer: app name (EssayGrader), copyright year, and "AI-powered essay feedback" tagline
   - Styling: border-t border-border, py-8, text-center, text-sm text-muted-foreground
   - GraduationCapIcon small next to the name
   - max-w-6xl mx-auto px-4

All components must use semantic Tailwind token classes exclusively (no hardcoded colors) so dark mode works automatically.
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All five landing section components exist, compile cleanly, use motion/react animations, Lucide icons, and Tailwind theme tokens. Each is a self-contained module with a single default or named export.</done>
</task>

<task type="auto">
  <name>Task 2: Wire section components into LandingPage and replace placeholder content</name>
  <files>src/pages/LandingPage.tsx</files>
  <action>
Update the LandingPage shell from Plan 01 to import and render all five section components.

1. Import: HeroSection, FeatureHighlights, HowItWorks, WalkthroughDemo, Footer from `@/components/landing/`
2. Replace the placeholder hero section with: `<HeroSection onSignIn={() => setShowSignIn(true)} onRegister={() => navigate("/register")} />`
3. Add remaining sections in order after the hero:
   - `<FeatureHighlights />`
   - `<HowItWorks />`
   - `<WalkthroughDemo />`
   - `<Footer />`
4. Keep the existing auth redirect logic (isSignedIn check, navigate to /grade) and SignInDialog at the bottom
5. Ensure the page has no horizontal overflow -- the LandingLayout provides the outer container, sections manage their own max-width
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>LandingPage renders all five sections in order. Sign In button in hero opens SignInDialog. Register button navigates to /register. Page scrolls smoothly through all sections with animations triggering on scroll.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete landing page</name>
  <action>
User verifies the complete landing page visually in the browser, including all sections, animations, auth flows, and dark mode.
  </action>
  <what-built>Complete landing page with hero, feature highlights, how-it-works, animated walkthrough demo, and footer. Sign-in modal flow and auth-based routing.</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Visit http://localhost:5173/ in a browser (ensure you are signed out)
    3. Verify landing page shows:
       - Minimal header with logo + Sign In button
       - Hero section with headline, subtitle, Sign In + Register buttons
       - Feature highlight cards (4 cards with icons)
       - How-it-works section (3 numbered steps)
       - Animated walkthrough demo (auto-playing grading flow animation)
       - Footer with app name and copyright
    4. Scroll down -- sections should animate in smoothly
    5. Click "Sign In" in the hero -- SignInDialog modal opens
    6. Sign in with valid credentials -- redirects to /grade (grading page)
    7. Visit http://localhost:5173/ again while signed in -- instant redirect to /grade (no flash)
    8. Sign out, visit http://localhost:5173/grade -- redirects to /
    9. Click "Register" -- navigates to /register (redirects back to / for now)
    10. Toggle dark mode -- verify all sections look correct
  </how-to-verify>
  <verify>Manual visual inspection</verify>
  <done>User approves the landing page design, animations, auth flow, and dark mode rendering.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. Landing page renders all 5 sections with proper content and styling
3. Scroll animations trigger correctly (fade-in on scroll)
4. Walkthrough demo auto-plays when scrolled into view
5. Dark mode renders correctly (no hardcoded colors)
6. Sign In button opens modal, successful auth redirects to /grade
7. Register button navigates to /register
8. All auth routing from Plan 01 still works
</verification>

<success_criteria>
- Landing page has hero, feature highlights, how-it-works, walkthrough demo, and footer sections
- All sections use scroll-triggered animations via motion/react
- Sign In and Register CTAs are prominent in the hero section (LAND-02)
- Landing page communicates product value with site description and feature highlights (LAND-01)
- Dark mode works correctly across all sections
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/16-landing-page-auth-entry/16-02-SUMMARY.md`
</output>
