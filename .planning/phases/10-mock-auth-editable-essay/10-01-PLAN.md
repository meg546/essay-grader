---
phase: 10-mock-auth-editable-essay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/profile-store.ts
  - src/pages/ProfilePage.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Profile page shows email+password sign-in form when user is not authenticated"
    - "Sign-in validates email format and password length (>= 6 chars), shows errors, and simulates async delay"
    - "After sign-in, profile page shows settings (grade level) and history cards"
    - "User can sign out and return to the sign-in form"
    - "Settings and history cards are hidden when user is not signed in"
  artifacts:
    - path: "src/stores/profile-store.ts"
      provides: "Async signIn with email+password validation, isSigningIn state"
      contains: "signIn: async"
    - path: "src/pages/ProfilePage.tsx"
      provides: "Password field, validation errors, conditional settings/history rendering"
      contains: "type=\"password\""
  key_links:
    - from: "src/pages/ProfilePage.tsx"
      to: "src/stores/profile-store.ts"
      via: "useProfileStore signIn call with email + password"
      pattern: "signIn\\(.*,.*\\)"
    - from: "src/stores/profile-store.ts"
      to: "src/api/delay.ts"
      via: "delay() import for simulated async"
      pattern: "await delay"
---

<objective>
Add mock email+password authentication to the Profile page.

Purpose: Give users a realistic sign-in experience with validation and async simulation, gating profile settings and history behind authentication.
Output: Updated profile store with async sign-in and ProfilePage with password field, validation, loading state, and conditional content rendering.
</objective>

<execution_context>
@/Users/matthewgardner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewgardner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mock-auth-editable-essay/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/stores/profile-store.ts (WILL BE MODIFIED):
```typescript
interface ProfileState {
  email: string;
  gradeLevel: GradeLevel;
  isSignedIn: boolean;
  signIn: (email: string) => void;  // Currently sync, email-only
  signOut: () => void;
  setGradeLevel: (level: GradeLevel) => void;
}
// Uses persist middleware with name "essay-grader-profile"
```

From src/api/delay.ts:
```typescript
export function delay(ms: number = 800): Promise<void>;
```

From src/pages/ProfilePage.tsx:
- Currently has email-only sign-in form (no password)
- Settings (Grade Level card) and History (Grading History card) always render regardless of auth state
- Uses shadcn Input, Button, Card, Select components
- Uses sonner for toasts (imported in GradingPage, available project-wide)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update profile store with async sign-in and validation</name>
  <files>src/stores/profile-store.ts</files>
  <action>
Modify the ProfileState interface and implementation in `src/stores/profile-store.ts`:

1. Add `isSigningIn: boolean` field to ProfileState interface (default: `false`)
2. Change `signIn` signature from `(email: string) => void` to `(email: string, password: string) => Promise<{ success: boolean; error?: string }>`
3. Implement async signIn:
   - Set `isSigningIn: true`
   - `await delay(1000)` (import from `@/api/delay`)
   - Validate email with regex: `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
   - If invalid email: set `isSigningIn: false`, return `{ success: false, error: "Invalid email format" }`
   - Validate password length >= 6
   - If too short: set `isSigningIn: false`, return `{ success: false, error: "Password must be at least 6 characters" }`
   - On success: set `{ email, isSignedIn: true, isSigningIn: false }`, return `{ success: true }`
4. Add `partialize` to the persist config to exclude `isSigningIn` from localStorage (prevents stuck loading state on refresh). Keep persisting `email`, `gradeLevel`, `isSignedIn`:
   ```
   partialize: (state) => ({
     email: state.email,
     gradeLevel: state.gradeLevel,
     isSignedIn: state.isSignedIn,
   })
   ```

Do NOT change `signOut` or `setGradeLevel` -- they remain as-is.
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Profile store has async signIn accepting email+password, validates both, simulates delay, returns success/error, excludes isSigningIn from persistence</done>
</task>

<task type="auto">
  <name>Task 2: Update ProfilePage with password field, validation errors, and conditional content</name>
  <files>src/pages/ProfilePage.tsx</files>
  <action>
Modify `src/pages/ProfilePage.tsx`:

1. Add `passwordInput` state alongside existing `emailInput` state: `const [passwordInput, setPasswordInput] = useState("")`
2. Add `error` state: `const [error, setError] = useState("")`
3. Also destructure `isSigningIn` from `useProfileStore()`

4. Update the sign-in form (inside the Account card's `!isSignedIn` branch):
   - Change from single-line `flex gap-2` to a vertical form with `space-y-3`
   - Keep the existing email Input (type="email", placeholder="you@example.com")
   - Add password Input: `type="password"`, `placeholder="Password"`, `minLength={6}`, bound to `passwordInput`
   - If `error` is non-empty, show it in a `<p className="text-sm text-destructive">{error}</p>`
   - Update form onSubmit to:
     ```
     e.preventDefault();
     setError("");
     const result = await signIn(emailInput.trim(), passwordInput);
     if (!result.success) setError(result.error ?? "Sign-in failed");
     ```
   - Update Submit button: disabled when `isSigningIn`, show Loader2 spinner with "Signing in..." text when loading, otherwise "Sign In"
   - Import `Loader2` from `lucide-react`

5. Gate the Grade Level card and Grading History card behind `isSignedIn`:
   - Wrap both cards in `{isSignedIn && (<> ... </>)}`
   - They should only render when the user is authenticated

6. Keep the Account card always visible (it shows sign-in form OR signed-in state).

The signed-in state inside the Account card (showing email + Sign Out button) remains unchanged.
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Profile page shows email+password form when not signed in, displays validation errors, shows loading during sign-in, hides settings/history behind auth, sign-out works and returns to form</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no type errors
2. Visit Profile page when not signed in: only Account card visible with email+password form
3. Submit with invalid email: error message shown
4. Submit with short password: error message shown
5. Submit with valid credentials: loading spinner, then signed-in state with settings and history visible
6. Click Sign Out: returns to sign-in form, settings/history hidden again
</verification>

<success_criteria>
- Profile page shows email+password sign-in form for unauthenticated users
- Validation errors display for bad email format or short password
- Simulated 1s delay with loading spinner during sign-in
- Settings and history cards appear only after successful sign-in
- Sign-out returns user to the sign-in form
- Build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-mock-auth-editable-essay/10-01-SUMMARY.md`
</output>
