---
phase: 11-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env
  - .gitignore
  - docker-compose.yml
  - backend/pyproject.toml
  - backend/Dockerfile
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/database.py
  - backend/app/deps.py
  - backend/app/models/__init__.py
  - backend/app/models/base.py
  - backend/app/schemas/__init__.py
  - backend/app/routes/__init__.py
  - backend/app/routes/health.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "FastAPI app starts and serves a health check response"
    - "Docker Compose brings up PostgreSQL with a healthy status"
    - "CORS headers are present on responses allowing the Vite dev server origin"
  artifacts:
    - path: "backend/app/main.py"
      provides: "FastAPI application with CORS middleware and router"
      exports: ["app"]
    - path: "backend/app/config.py"
      provides: "Settings loaded from .env"
      exports: ["Settings", "get_settings"]
    - path: "backend/app/database.py"
      provides: "Async SQLAlchemy engine and session factory"
      exports: ["engine", "async_session_factory"]
    - path: "backend/app/deps.py"
      provides: "FastAPI dependency for async DB sessions"
      exports: ["get_db"]
    - path: "docker-compose.yml"
      provides: "PostgreSQL service with health check + backend service (profile: full)"
    - path: ".env"
      provides: "DATABASE_URL, CORS_ORIGINS, DEBUG configuration"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/config.py"
      via: "get_settings() call for CORS origins"
      pattern: "get_settings.*cors_origins"
    - from: "backend/app/database.py"
      to: "backend/app/config.py"
      via: "get_settings() for database URL"
      pattern: "get_settings.*database_url"
    - from: "docker-compose.yml"
      to: ".env"
      via: "env_file directive"
      pattern: "env_file"
---

<objective>
Scaffold the FastAPI backend project with Docker Compose, PostgreSQL, and a working health endpoint with CORS.

Purpose: Establish the backend infrastructure shell that all subsequent phases (auth, grading, persistence) build on.
Output: A running FastAPI server accessible at http://localhost:8000/api/health with PostgreSQL available via Docker Compose.
</objective>

<execution_context>
@/Users/matthewgardner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewgardner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-backend-foundation/11-CONTEXT.md
@.planning/phases/11-backend-foundation/11-RESEARCH.md
@src/api/types.ts

<interfaces>
<!-- Frontend TypeScript types the backend must eventually serialize to (camelCase JSON) -->
<!-- These are reference only for this plan; Plan 02 creates the matching Pydantic schemas -->

From src/api/types.ts:
```typescript
export interface GradingResult {
  id: string;
  essayText: string;
  essayExcerpt: string;
  overallScore: number;
  maxScore: number;
  summary: string;
  categories: CategoryScore[];
  gradedAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend project scaffold with uv, FastAPI app, config, and database layer</name>
  <files>
    backend/pyproject.toml
    backend/app/__init__.py
    backend/app/main.py
    backend/app/config.py
    backend/app/database.py
    backend/app/deps.py
    backend/app/models/__init__.py
    backend/app/models/base.py
    backend/app/schemas/__init__.py
    backend/app/routes/__init__.py
    backend/app/routes/health.py
    .env
    .gitignore
  </files>
  <action>
    1. Initialize the Python project:
       ```
       mkdir -p backend && cd backend
       uv init --app
       uv add "fastapi[standard]" "sqlalchemy[asyncio]" asyncpg alembic pydantic-settings
       ```

    2. Create `backend/app/config.py` with pydantic-settings BaseSettings:
       - `database_url: str` defaulting to `"postgresql+asyncpg://postgres:postgres@localhost:5432/essaygrader"`
       - `cors_origins: list[str]` defaulting to `["http://localhost:5173"]`
       - `debug: bool = False`
       - `env_file` pointing to repo root `.env` using `Path(__file__).resolve().parent.parent.parent / ".env"`
       - `extra="ignore"` in SettingsConfigDict
       - `get_settings()` function with `@lru_cache` decorator

    3. Create `backend/app/database.py`:
       - `create_async_engine` using `get_settings().database_url` with `echo=settings.debug`
       - `async_session_factory` via `async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)`

    4. Create `backend/app/deps.py`:
       - `async def get_db()` generator yielding `AsyncSession` from `async_session_factory`

    5. Create `backend/app/models/base.py`:
       - `class Base(DeclarativeBase): pass`

    6. Create `backend/app/main.py`:
       - FastAPI app with `title="Essay Grader API"`
       - CORSMiddleware with origins from `get_settings().cors_origins`, credentials=True, all methods/headers
       - Use `APIRouter(prefix="/api")` approach (not root_path) to avoid OpenAPI docs path issues
       - Include health router on the api_router
       - Include api_router on the app

    7. Create `backend/app/routes/health.py`:
       - `GET /health` returning `{"status": "ok"}`

    8. Create `.env` at repo root:
       ```
       DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/essaygrader
       CORS_ORIGINS=["http://localhost:5173"]
       DEBUG=true
       ```

    9. Append to `.gitignore` (do not overwrite existing content):
       - `backend/.venv/`
       - `backend/__pycache__/`
       - `.env` (if not already present)

    10. Create empty `__init__.py` files in `backend/app/`, `backend/app/models/`, `backend/app/schemas/`, `backend/app/routes/`.

    IMPORTANT: Use `APIRouter(prefix="/api")` per research recommendation -- not `root_path="/api"`. This avoids OpenAPI docs URL issues.
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader/backend && uv run python -c "from app.main import app; print('App created:', app.title)"</automated>
  </verify>
  <done>FastAPI app imports successfully, config loads from .env, database module creates async engine, health route is registered at /api/health</done>
</task>

<task type="auto">
  <name>Task 2: Create Docker Compose with PostgreSQL and backend services, verify health endpoint</name>
  <files>
    docker-compose.yml
    backend/Dockerfile
  </files>
  <action>
    1. Create `docker-compose.yml` at repo root with two services:

       **db service:**
       - Image: `postgres:16-alpine`
       - Environment: POSTGRES_USER=postgres, POSTGRES_PASSWORD=postgres, POSTGRES_DB=essaygrader
       - Ports: 5432:5432
       - Volume: pgdata:/var/lib/postgresql/data
       - Healthcheck: `pg_isready -U postgres` with interval=5s, timeout=3s, retries=5

       **backend service:**
       - Build context: `./backend`
       - Ports: 8000:8000
       - env_file: .env
       - depends_on db with `condition: service_healthy`
       - profiles: ["full"] (only starts with `docker compose --profile full up`)

       **volumes:** pgdata (named volume)

    2. Create `backend/Dockerfile`:
       ```dockerfile
       FROM python:3.12-slim
       COPY --from=ghcr.io/astral-sh/uv:latest /uv /uvx /bin/
       COPY . /app
       WORKDIR /app
       RUN uv sync --frozen --no-cache
       EXPOSE 8000
       CMD ["/app/.venv/bin/uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
       ```

    3. Start the db service and verify it's healthy:
       ```
       docker compose up -d db
       docker compose ps  # Verify db shows "healthy"
       ```

    4. Start the FastAPI dev server natively (hybrid dev workflow per user decision):
       ```
       cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 &
       ```

    5. Verify health endpoint responds:
       ```
       curl -s http://localhost:8000/api/health
       # Expected: {"status":"ok"}
       ```

    6. Verify CORS headers:
       ```
       curl -s -H "Origin: http://localhost:5173" -I http://localhost:8000/api/health
       # Expected: access-control-allow-origin: http://localhost:5173
       ```

    7. Stop the dev server and leave db running for Plan 02.
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && docker compose up -d db && sleep 3 && docker compose ps --format json | grep -q healthy && echo "DB healthy"</automated>
  </verify>
  <done>PostgreSQL container is healthy on port 5432, docker-compose.yml defines both db and backend services, Dockerfile builds the backend image, health endpoint at /api/health returns {"status":"ok"} with CORS headers</done>
</task>

</tasks>

<verification>
1. `cd backend && uv run python -c "from app.main import app; print(app.title)"` prints "Essay Grader API"
2. `docker compose up -d db && docker compose ps` shows db as healthy
3. `cd backend && uv run uvicorn app.main:app --port 8000` starts without errors
4. `curl http://localhost:8000/api/health` returns `{"status":"ok"}`
5. `curl -H "Origin: http://localhost:5173" -I http://localhost:8000/api/health` includes `access-control-allow-origin: http://localhost:5173`
</verification>

<success_criteria>
- FastAPI project scaffold exists at backend/ with uv-managed dependencies
- Config loads DATABASE_URL, CORS_ORIGINS, DEBUG from repo-root .env
- Async database engine and session factory are configured
- Health endpoint responds at http://localhost:8000/api/health
- CORS allows http://localhost:5173 origin
- Docker Compose starts PostgreSQL with health check
- Dockerfile builds the backend image for demo/CI use
</success_criteria>

<output>
After completion, create `.planning/phases/11-backend-foundation/11-01-SUMMARY.md`
</output>
