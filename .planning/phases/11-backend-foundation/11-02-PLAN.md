---
phase: 11-backend-foundation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - backend/app/schemas/base.py
  - backend/app/schemas/grading.py
  - backend/app/schemas/history.py
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_health.py
  - backend/tests/test_cors.py
  - backend/tests/test_schemas.py
autonomous: true
requirements:
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Pydantic schemas serialize to camelCase JSON matching frontend TypeScript types exactly"
    - "Alembic can generate and apply migrations against the running PostgreSQL instance"
    - "All automated tests pass confirming health endpoint, CORS, and schema serialization"
  artifacts:
    - path: "backend/app/schemas/base.py"
      provides: "CamelModel base class with alias_generator=to_camel"
      exports: ["CamelModel"]
    - path: "backend/app/schemas/grading.py"
      provides: "GradingResult, CategoryScore, HighlightRange Pydantic schemas"
      exports: ["GradingResult", "CategoryScore", "HighlightRange"]
    - path: "backend/app/schemas/history.py"
      provides: "HistoryItem Pydantic schema"
      exports: ["HistoryItem"]
    - path: "backend/alembic/env.py"
      provides: "Async Alembic environment importing Base metadata"
    - path: "backend/tests/test_schemas.py"
      provides: "Tests verifying camelCase serialization matches frontend types"
  key_links:
    - from: "backend/app/schemas/grading.py"
      to: "src/api/types.ts"
      via: "camelCase JSON output must match TS interface field names"
      pattern: "essayText|overallScore|maxScore|categoryId|gradedAt"
    - from: "backend/alembic/env.py"
      to: "backend/app/models/base.py"
      via: "target_metadata = Base.metadata"
      pattern: "target_metadata.*Base\\.metadata"
    - from: "backend/alembic/env.py"
      to: "backend/app/config.py"
      via: "get_settings() for database URL"
      pattern: "get_settings.*database_url"
---

<objective>
Create Pydantic response schemas matching frontend TypeScript types with camelCase serialization, set up Alembic migrations, and add a test suite proving everything works.

Purpose: Ensure the backend can produce JSON that the frontend consumes without transformation, and that database schema evolution is managed via Alembic.
Output: Pydantic schemas passing camelCase serialization tests, Alembic configured with an initial migration applied to PostgreSQL, full test suite green.
</objective>

<execution_context>
@/Users/matthewgardner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewgardner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-backend-foundation/11-CONTEXT.md
@.planning/phases/11-backend-foundation/11-RESEARCH.md
@.planning/phases/11-backend-foundation/11-01-SUMMARY.md
@src/api/types.ts

<interfaces>
<!-- Frontend TypeScript types that Pydantic schemas MUST match exactly (camelCase JSON) -->

From src/api/types.ts:
```typescript
export interface HighlightRange {
  start: number;
  end: number;
  categoryId: string;
  type: "strength" | "improvement";
  feedback: string;
}

export interface CategoryScore {
  id: string;
  name: string;
  score: number;
  maxScore: number;
  strengths: string[];
  improvements: string[];
  justification: string;
  highlights: HighlightRange[];
}

export interface GradingResult {
  id: string;
  essayText: string;
  essayExcerpt: string;
  overallScore: number;
  maxScore: number;
  summary: string;
  categories: CategoryScore[];
  gradedAt: string;
}

export interface HistoryItem {
  id: string;
  essayExcerpt: string;
  overallScore: number;
  maxScore: number;
  categoryCount: number;
  gradedAt: string;
}
```

<!-- From Plan 01 (expected to exist): -->
From backend/app/config.py:
```python
@lru_cache
def get_settings() -> Settings:
    return Settings()
```

From backend/app/models/base.py:
```python
class Base(DeclarativeBase):
    pass
```

From backend/app/database.py:
```python
engine = create_async_engine(settings.database_url, echo=settings.debug)
async_session_factory = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Pydantic camelCase schemas matching frontend types with test suite</name>
  <files>
    backend/app/schemas/base.py
    backend/app/schemas/grading.py
    backend/app/schemas/history.py
    backend/tests/__init__.py
    backend/tests/conftest.py
    backend/tests/test_health.py
    backend/tests/test_cors.py
    backend/tests/test_schemas.py
  </files>
  <behavior>
    - Test: GradingResult.model_dump(by_alias=True) produces keys exactly matching TS interface: id, essayText, essayExcerpt, overallScore, maxScore, summary, categories, gradedAt
    - Test: CategoryScore.model_dump(by_alias=True) produces keys: id, name, score, maxScore, strengths, improvements, justification, highlights
    - Test: HighlightRange.model_dump(by_alias=True) produces keys: start, end, categoryId, type, feedback
    - Test: HistoryItem.model_dump(by_alias=True) produces keys: id, essayExcerpt, overallScore, maxScore, categoryCount, gradedAt
    - Test: Nested serialization works (GradingResult with categories containing highlights all use camelCase)
    - Test: Models can be instantiated with snake_case field names (populate_by_name=True)
    - Test: GET /api/health returns 200 with {"status": "ok"}
    - Test: CORS preflight to /api/health with Origin http://localhost:5173 returns correct headers
  </behavior>
  <action>
    1. Add dev dependencies:
       ```
       cd backend && uv add --dev pytest pytest-asyncio httpx
       ```

    2. Create `backend/app/schemas/base.py`:
       - CamelModel base class inheriting from BaseModel
       - `model_config = ConfigDict(alias_generator=to_camel, populate_by_name=True, from_attributes=True)`
       - Import `to_camel` from `pydantic.alias_generators`

    3. Create `backend/app/schemas/grading.py` (all inherit CamelModel):
       - `HighlightRange`: start (int), end (int), category_id (str), type (str), feedback (str)
       - `CategoryScore`: id (str), name (str), score (float), max_score (float), strengths (list[str]), improvements (list[str]), justification (str), highlights (list[HighlightRange])
       - `GradingResult`: id (str), essay_text (str), essay_excerpt (str), overall_score (float), max_score (float), summary (str), categories (list[CategoryScore]), graded_at (str)

    4. Create `backend/app/schemas/history.py`:
       - `HistoryItem`: id (str), essay_excerpt (str), overall_score (float), max_score (float), category_count (int), graded_at (str)

    5. Create `backend/tests/__init__.py` (empty).

    6. Create `backend/tests/conftest.py`:
       - Fixture `client` that creates httpx.AsyncClient with `ASGITransport(app=app)` and base_url="http://test"
       - Use `@pytest.fixture` with async context manager

    7. Create `backend/tests/test_schemas.py`:
       - Test each schema's `model_dump(by_alias=True)` output keys match the exact TypeScript interface field names
       - Test nested serialization (GradingResult with full category + highlight tree)
       - Test instantiation with snake_case names works

    8. Create `backend/tests/test_health.py`:
       - Test `GET /api/health` returns 200 and `{"status": "ok"}`

    9. Create `backend/tests/test_cors.py`:
       - Test OPTIONS preflight with Origin: http://localhost:5173 returns access-control-allow-origin header

    10. Add pytest config to `backend/pyproject.toml`:
        ```toml
        [tool.pytest.ini_options]
        asyncio_mode = "auto"
        ```
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader/backend && uv run pytest tests/ -x -v</automated>
  </verify>
  <done>All schema tests pass confirming camelCase serialization matches frontend TS types exactly. Health and CORS tests pass. Models instantiable with both snake_case and camelCase field names.</done>
</task>

<task type="auto">
  <name>Task 2: Initialize Alembic with async template and create initial migration</name>
  <files>
    backend/alembic.ini
    backend/alembic/env.py
    backend/alembic/script.py.mako
    backend/alembic/versions/
  </files>
  <action>
    1. Initialize Alembic with async template:
       ```
       cd backend && uv run alembic init --template async alembic
       ```

    2. Edit `backend/alembic.ini`:
       - Remove or comment out the `sqlalchemy.url` line (will be loaded from env in env.py)
       - Keep all other defaults

    3. Edit `backend/alembic/env.py`:
       - Import `Base` from `app.models.base`
       - Import `get_settings` from `app.config`
       - Set `target_metadata = Base.metadata`
       - In `run_async_migrations()`, get the database URL from `get_settings().database_url`
       - Use `config.set_main_option("sqlalchemy.url", settings.database_url)` to override
       - Make sure all model modules are imported so autogenerate can detect tables
       - Note: No application tables exist yet (just Base). The initial migration will be empty or contain a minimal placeholder. Phase 12+ will add User, GradingResult tables.

    4. Create an initial migration to verify the pipeline works:
       ```
       cd backend && uv run alembic revision --autogenerate -m "initial_empty"
       ```
       This should produce a migration file (likely with empty upgrade/downgrade since no models define tables yet).

    5. Apply the migration against the running PostgreSQL (started in Plan 01):
       ```
       cd backend && uv run alembic upgrade head
       ```

    6. Verify Alembic tracking works:
       ```
       cd backend && uv run alembic current
       # Should show the initial migration revision as head
       ```
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader/backend && uv run alembic current 2>&1 | grep -q "head" && echo "Alembic at head"</automated>
  </verify>
  <done>Alembic initialized with async template, env.py loads database URL from settings, initial migration generated and applied to PostgreSQL, `alembic current` shows head revision</done>
</task>

</tasks>

<verification>
1. `cd backend && uv run pytest tests/ -v` -- all tests pass (health, CORS, schema serialization)
2. `cd backend && uv run python -c "from app.schemas.grading import GradingResult; print(GradingResult.model_json_schema())"` -- shows camelCase field names
3. `cd backend && uv run alembic current` -- shows head revision
4. `cd backend && uv run alembic upgrade head` -- runs without error (idempotent)
</verification>

<success_criteria>
- CamelModel base class provides camelCase alias generation for all schemas
- GradingResult, CategoryScore, HighlightRange, HistoryItem Pydantic schemas produce JSON keys matching frontend TypeScript interfaces exactly
- Alembic async template initialized with env.py loading DB URL from settings
- Initial migration generated and applied to PostgreSQL
- Test suite covers health endpoint, CORS headers, and schema serialization
- All tests pass: `cd backend && uv run pytest tests/ -x -v`
</success_criteria>

<output>
After completion, create `.planning/phases/11-backend-foundation/11-02-SUMMARY.md`
</output>
