---
phase: 12-authentication
plan: 01
subsystem: auth
tags: [jwt, pyjwt, pwdlib, argon2, bcrypt, sqlalchemy, alembic, postgresql]

# Dependency graph
requires:
  - phase: 11-backend-foundation
    provides: SQLAlchemy Base model, Alembic setup, Settings/config, async database engine
provides:
  - SQLAlchemy User model with UUID id, email, hashed_password, created_at
  - Alembic migration creating users table with unique email index
  - Password hashing (argon2/bcrypt) via hash_password/verify_password
  - JWT token creation/verification via create_access_token/decode_access_token
  - JWT config settings (jwt_secret, jwt_algorithm, jwt_expire_hours)
affects: [12-authentication, 13-llm-grading, 14-api-integration]

# Tech tracking
tech-stack:
  added: [pyjwt, pwdlib, python-multipart, argon2-cffi, bcrypt]
  patterns: [password hashing with argon2 preferred fallback to bcrypt, HS256 JWT with configurable expiry]

key-files:
  created:
    - backend/app/models/user.py
    - backend/app/auth/__init__.py
    - backend/app/auth/passwords.py
    - backend/app/auth/tokens.py
    - backend/alembic/versions/ad42fbd06e81_add_users_table.py
    - backend/tests/test_auth_utils.py
  modified:
    - backend/pyproject.toml
    - backend/app/config.py
    - backend/app/models/__init__.py
    - backend/alembic/env.py

key-decisions:
  - "Used autogenerate for Alembic migration since PostgreSQL was available (unlike phase 11)"
  - "Re-exported auth functions from app.auth.__init__ for convenient imports"

patterns-established:
  - "Auth utilities pattern: standalone functions importable from app.auth"
  - "User model pattern: UUID primary key, server_default for timestamps"

requirements-completed: [AUTH-01, AUTH-02]

# Metrics
duration: 2min
completed: 2026-03-09
---

# Phase 12 Plan 01: User Model and Auth Utilities Summary

**SQLAlchemy User model with UUID/email/password, argon2 password hashing via pwdlib, and HS256 JWT tokens via PyJWT**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-09T21:43:05Z
- **Completed:** 2026-03-09T21:44:54Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments
- User SQLAlchemy model with UUID id, unique indexed email, hashed_password, and server-default created_at
- Alembic autogenerated migration creating users table with unique email index
- Password hashing using pwdlib with argon2 (preferred) and bcrypt fallback
- JWT access token creation and decoding with configurable secret, algorithm, and expiry
- 7 new tests covering all auth utility behaviors, 18 total tests passing

## Task Commits

Each task was committed atomically:

1. **Task 1: Add dependencies, update config, create User model and migration** - `8da2e7e` (feat)
2. **Task 2 RED: Failing tests for auth utilities** - `f5e8112` (test)
3. **Task 2 GREEN: Implement password hashing and JWT tokens** - `7191109` (feat)

## Files Created/Modified
- `backend/app/models/user.py` - SQLAlchemy User model with id, email, hashed_password, created_at
- `backend/app/models/__init__.py` - Re-exports Base and User for convenient imports
- `backend/app/auth/__init__.py` - Re-exports hash_password, verify_password, create_access_token, decode_access_token
- `backend/app/auth/passwords.py` - Password hashing and verification using pwdlib
- `backend/app/auth/tokens.py` - JWT creation and decoding using PyJWT
- `backend/alembic/versions/ad42fbd06e81_add_users_table.py` - Migration creating users table
- `backend/tests/test_auth_utils.py` - 7 tests for password and JWT utilities
- `backend/pyproject.toml` - Added pyjwt, pwdlib, python-multipart dependencies
- `backend/app/config.py` - Added jwt_secret, jwt_algorithm, jwt_expire_hours settings
- `backend/alembic/env.py` - Added User model import for autogenerate detection

## Decisions Made
- Used Alembic autogenerate since PostgreSQL was available (unlike phase 11 which used manual migration)
- Re-exported all auth functions from `app.auth.__init__` for convenient `from app.auth import hash_password` usage

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- User model and auth primitives ready for auth routes (12-02)
- Registration, login, and token-protected endpoints can now use hash_password, verify_password, create_access_token, decode_access_token
- InsecureKeyLengthWarning from PyJWT on default "change-me-in-production" secret is expected; production deployments must set JWT_SECRET env var to a 32+ byte key

## Self-Check: PASSED

All 7 created files verified present. All 3 task commits verified in git log.

---
*Phase: 12-authentication*
*Completed: 2026-03-09*
