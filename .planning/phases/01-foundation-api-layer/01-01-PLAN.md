---
phase: 01-foundation-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - vite.config.ts
  - src/index.css
  - src/main.tsx
  - src/App.tsx
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/sheet.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Layout.tsx
  - src/pages/LandingPage.tsx
  - src/pages/GradingPage.tsx
  - src/pages/ResultsPage.tsx
  - src/pages/HistoryPage.tsx
autonomous: true
requirements:
  - NAVL-02
  - NAVL-04
  - NAVL-05

must_haves:
  truths:
    - "Running npm run dev serves the app with a visible layout shell (header, content area) styled with warm sage/cream palette"
    - "User can navigate between all four page routes (landing /, grading /grade, results /results, history /history) via header navigation"
    - "Each placeholder page shows skeleton mockups hinting at future layout, not just text titles"
    - "Layout responds correctly at 768px tablet breakpoint -- tabs collapse to hamburger menu, no horizontal scrolling"
  artifacts:
    - path: "src/index.css"
      provides: "OKLCH CSS custom properties for light and dark themes"
      contains: "--primary: oklch"
    - path: "src/App.tsx"
      provides: "React Router v7 with 4 routes wrapped in Layout"
      exports: ["default"]
    - path: "src/components/layout/Header.tsx"
      provides: "Top bar with tab nav (desktop) and Sheet hamburger (mobile)"
      min_lines: 30
    - path: "src/components/layout/Layout.tsx"
      provides: "Layout wrapper with Header + centered max-width Outlet"
      min_lines: 10
    - path: "src/pages/GradingPage.tsx"
      provides: "Skeleton placeholder for grading page"
      min_lines: 10
  key_links:
    - from: "src/App.tsx"
      to: "src/components/layout/Layout.tsx"
      via: "Route element wrapper"
      pattern: "Route.*element.*Layout"
    - from: "src/components/layout/Header.tsx"
      to: "react-router"
      via: "NavLink components for tab navigation"
      pattern: "NavLink.*to="
    - from: "src/index.css"
      to: "src/components/layout/Layout.tsx"
      via: "CSS variables consumed by Tailwind classes"
      pattern: "bg-background|text-foreground"
---

<objective>
Scaffold the React/TypeScript project with Vite, configure shadcn/ui with the custom warm sage/cream design system, set up React Router v7 with four routes, build the responsive layout shell (Header with tab nav + mobile hamburger), and create skeleton placeholder pages for all four routes.

Purpose: Establishes the entire project foundation -- build tooling, design tokens, routing, and layout -- that every subsequent phase builds on.
Output: A running Vite dev server showing a styled layout shell with navigable routes and skeleton placeholders.
</objective>

<execution_context>
@/Users/matthewgardner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewgardner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-api-layer/01-CONTEXT.md
@.planning/phases/01-foundation-api-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project, install dependencies, configure shadcn/ui with custom warm earthy theme</name>
  <files>package.json, tsconfig.json, tsconfig.app.json, vite.config.ts, src/index.css, src/main.tsx, src/lib/utils.ts, src/components/ui/button.tsx, src/components/ui/sheet.tsx, components.json</files>
  <action>
1. Scaffold the project using `npm create vite@latest . -- --template react-ts` (use `.` since we are already in the project directory, or scaffold into a temp dir and move files if the directory is not empty -- handle the .planning directory gracefully).

2. Install dependencies: `npm install react-router zustand` and `npm install -D @types/node`

3. Run `npx shadcn@latest init` to set up shadcn/ui. This configures Tailwind v4, path aliases (@/), and the cn() utility automatically. Choose "New York" style if prompted. If the init command asks about CSS variables, accept defaults -- we will override them next.

4. Add required shadcn components: `npx shadcn@latest add button sheet`

5. Replace the contents of `src/index.css` with the full OKLCH CSS custom property theme from the RESEARCH.md file (Pattern 3). This includes:
   - `@import "tailwindcss";`
   - `@custom-variant dark (&:is(.dark *));`
   - `@theme inline` block mapping all color/radius variables
   - `:root` block with warm cream/sage light theme OKLCH values (background ~0.97, primary sage ~0.55 hue 145, etc.)
   - `.dark` block with dark theme OKLCH values
   - Set `--radius: 0.75rem` for rounded-lg/xl corners per user decision
   - Add base body styles if needed: `body { font-family: system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif; }` for the system font stack (per user decision: Inter/system stack, no external font dependencies)

6. Verify the project builds: `npm run build` should succeed with no errors.

IMPORTANT: Do NOT use hardcoded color values anywhere in components. ALL colors must come from CSS variables via Tailwind classes (bg-primary, text-foreground, etc.). This is critical for dark mode support in Phase 5.

Note: If `npx shadcn@latest init` generates a Tailwind v4 CSS file that already has some theme variables, merge our custom OKLCH values into it rather than replacing blindly -- keep any structural setup shadcn adds.
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Project scaffolded with Vite + React 19 + TypeScript. shadcn/ui initialized with button and sheet components. index.css contains full OKLCH light/dark CSS custom properties with warm sage/cream palette. npm run build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create layout shell with responsive navigation, React Router setup, and skeleton placeholder pages</name>
  <files>src/App.tsx, src/components/layout/Layout.tsx, src/components/layout/Header.tsx, src/pages/LandingPage.tsx, src/pages/GradingPage.tsx, src/pages/ResultsPage.tsx, src/pages/HistoryPage.tsx</files>
  <action>
1. Create `src/components/layout/Layout.tsx`:
   - Wraps content in `min-h-screen bg-background text-foreground`
   - Renders `<Header />` at top
   - Renders `<main className="mx-auto max-w-[960px] px-4 py-8"><Outlet /></main>` for centered content area (~960px max-width per user decision of ~900-1000px)
   - Import Outlet from "react-router"

2. Create `src/components/layout/Header.tsx`:
   - Top bar with app branding on the left (e.g., "EssayGrader" text logo in primary color with a subtle icon or just text -- Claude's discretion)
   - Desktop navigation (visible at `md:` breakpoint and up): horizontal tab-style NavLink items for Landing, Grade, Results, History
   - Style active NavLink with `bg-accent text-accent-foreground` and inactive with hover states. Use rounded-lg for tab feel.
   - Mobile navigation (visible below `md:`): hamburger button that opens a shadcn Sheet (slide-out from left) with vertical nav links
   - CRITICAL: Close the Sheet after navigation. Control the Sheet's `open` state and set it to `false` in each NavLink's onClick handler (per Pitfall 3 in research).
   - Use `useLocation` from react-router to highlight the active route in both desktop and mobile nav
   - Routes: "/" (Landing), "/grade" (Grade), "/results" (Results), "/history" (History)

3. Create `src/App.tsx`:
   - Import BrowserRouter, Routes, Route from "react-router"
   - Wrap Routes in BrowserRouter
   - Use Layout as the parent Route element
   - Define 4 child routes: / -> LandingPage, /grade -> GradingPage, /results -> ResultsPage, /history -> HistoryPage

4. Create 4 placeholder pages with skeleton mockups (NOT just text titles -- per user decision):

   `src/pages/LandingPage.tsx`: Skeleton suggesting hero section (tall box), description area (medium box), and CTA button (small box)

   `src/pages/GradingPage.tsx`: Skeleton suggesting textarea on left + rubric rows on right (grid md:grid-cols-2 layout), submit button at bottom. This hints at the Phase 2 layout.

   `src/pages/ResultsPage.tsx`: Skeleton suggesting summary paragraph area (wide box), score bars (3-4 horizontal bars of varying width), and expandable feedback sections (stacked cards)

   `src/pages/HistoryPage.tsx`: Skeleton suggesting a data table with header row and 4-5 data rows

   All skeleton boxes should use `bg-muted rounded-lg` or `bg-muted rounded-xl` with appropriate heights. Include a subtle page title skeleton at the top of each page.

5. Update `src/main.tsx` if needed to render `<App />` (should already be set up by Vite scaffold).

6. Verify responsive behavior: The layout must not produce horizontal scrolling at any width >= 320px. At >= 768px (md breakpoint), desktop tab nav is visible. Below 768px, only the hamburger icon shows.
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Layout shell renders with Header (app branding + tab nav on desktop, hamburger/Sheet on mobile). All 4 routes navigable via NavLinks. Each placeholder page shows skeleton mockups suggesting future layout. No horizontal scrolling at tablet breakpoint. Active route highlighted in navigation.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts successfully and serves the app
2. Navigating to /, /grade, /results, /history each shows a distinct skeleton placeholder page
3. Header shows tab-style navigation on desktop (>= 768px) with active state highlighting
4. Header shows hamburger icon on mobile (< 768px) that opens Sheet drawer with nav links
5. Sheet closes after clicking a nav link on mobile
6. All colors use CSS variable-backed Tailwind classes (no hardcoded hex/rgb values)
7. Warm sage/cream palette is visually apparent (sage green header accents, cream background)
8. `npm run build` produces zero errors
</verification>

<success_criteria>
- App runs with `npm run dev` showing styled layout shell
- All 4 routes accessible via navigation with skeleton placeholder content
- Responsive: desktop tabs, mobile hamburger/sheet
- Warm sage/cream OKLCH color palette applied throughout
- CSS custom properties support future dark mode (both :root and .dark defined)
- Zero build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-layer/01-01-SUMMARY.md`
</output>
