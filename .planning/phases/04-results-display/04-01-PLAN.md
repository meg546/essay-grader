---
phase: 04-results-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/score-utils.ts
  - src/components/ui/collapsible.tsx
  - src/components/results/ScoreBar.tsx
  - src/components/results/ResultsSummary.tsx
  - src/components/results/ScoreOverview.tsx
  - src/components/results/CategoryFeedback.tsx
  - src/pages/ResultsPage.tsx
autonomous: true
requirements: [RSLT-01, RSLT-02, RSLT-03, RSLT-04]

must_haves:
  truths:
    - "User sees overall summary paragraph at top of results page"
    - "User sees per-category color-coded score bars (green >= 70%, yellow >= 40%, red < 40%)"
    - "User sees aggregate total score prominently displayed"
    - "User can expand and collapse per-category feedback sections showing strengths, improvements, and justification"
    - "User sees friendly empty state when navigating to /results/:id without data in store"
  artifacts:
    - path: "src/lib/score-utils.ts"
      provides: "Score color/level utility functions"
      exports: ["getScoreLevel", "getScoreBarColor", "getScoreTextColor"]
    - path: "src/components/results/ScoreBar.tsx"
      provides: "Single category horizontal score bar"
      exports: ["ScoreBar"]
    - path: "src/components/results/ResultsSummary.tsx"
      provides: "Summary card with aggregate score and summary text"
      exports: ["ResultsSummary"]
    - path: "src/components/results/ScoreOverview.tsx"
      provides: "All score bars in a section"
      exports: ["ScoreOverview"]
    - path: "src/components/results/CategoryFeedback.tsx"
      provides: "Collapsible feedback card for one category"
      exports: ["CategoryFeedback"]
    - path: "src/pages/ResultsPage.tsx"
      provides: "Complete results page composing all components"
      contains: "useAppStore"
  key_links:
    - from: "src/pages/ResultsPage.tsx"
      to: "src/stores/app-store.ts"
      via: "useAppStore((s) => s.currentResult)"
      pattern: "useAppStore.*currentResult"
    - from: "src/components/results/ScoreBar.tsx"
      to: "src/lib/score-utils.ts"
      via: "getScoreLevel + getScoreBarColor"
      pattern: "getScoreLevel|getScoreBarColor"
    - from: "src/components/results/CategoryFeedback.tsx"
      to: "src/components/ui/collapsible.tsx"
      via: "Collapsible/CollapsibleTrigger/CollapsibleContent"
      pattern: "Collapsible"
---

<objective>
Build the complete results display page with summary, color-coded score bars, aggregate score, and expandable per-category feedback sections.

Purpose: Replace the skeleton ResultsPage placeholder with a fully functional grading results display that reads from the Zustand store (populated by Phase 3 submission flow).
Output: 5 new component/utility files + updated ResultsPage composing them all.
</objective>

<execution_context>
@/Users/matthewgardner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matthewgardner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/api/types.ts:
```typescript
export interface CategoryScore {
  name: string;
  score: number;
  maxScore: number;
  strengths: string[];
  improvements: string[];
  justification: string;
}

export interface GradingResult {
  id: string;
  essayExcerpt: string;
  overallScore: number;
  maxScore: number;
  summary: string;
  categories: CategoryScore[];
  gradedAt: string;
}
```

From src/stores/app-store.ts:
```typescript
currentResult: GradingResult | null;
// Access via: useAppStore((s) => s.currentResult)
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
```

Available shadcn components: button, card (Card/CardHeader/CardTitle/CardContent), sheet, input, label, textarea, sonner.
Need to install: collapsible (via `npx shadcn@latest add collapsible`).
Icon library: lucide-react (ChevronDown, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create score utilities, install Collapsible, build ScoreBar and ResultsSummary</name>
  <files>src/lib/score-utils.ts, src/components/ui/collapsible.tsx, src/components/results/ScoreBar.tsx, src/components/results/ResultsSummary.tsx, src/components/results/ScoreOverview.tsx</files>
  <action>
1. Install shadcn Collapsible component: `npx shadcn@latest add collapsible` (this creates src/components/ui/collapsible.tsx).

2. Create `src/lib/score-utils.ts` with three exports:
   - `getScoreLevel(score, maxScore)` returns `"high" | "medium" | "low"` based on percentage (>=70% high, >=40% medium, <40% low). Handle maxScore=0 edge case (return "low").
   - `getScoreBarColor(level)` maps: high -> "bg-emerald-500", medium -> "bg-amber-400", low -> "bg-rose-500"
   - `getScoreTextColor(level)` maps: high -> "text-emerald-700", medium -> "text-amber-600", low -> "text-rose-600"

3. Create `src/components/results/ScoreBar.tsx`:
   - Props: `{ name: string; score: number; maxScore: number }`
   - Layout: category name (w-40 truncate), horizontal bar (flex-1, h-6, rounded-lg, bg-muted background), score fraction (w-12, right-aligned)
   - Bar fill uses inline `style={{ width: `${pct}%` }}` (NOT dynamic Tailwind classes — they get purged)
   - Fill color from getScoreBarColor(getScoreLevel(score, maxScore))
   - Add `transition-all duration-700 ease-out` on the fill div for smooth animation

4. Create `src/components/results/ResultsSummary.tsx`:
   - Props: `{ result: GradingResult }`
   - Uses shadcn Card. Contains:
     - Aggregate score displayed large: `{overallScore}` in 4xl bold with getScoreTextColor, then `/ {maxScore}` in muted, then `({pct}%)` in score color
     - Summary paragraph below in normal text
   - This satisfies RSLT-01 (summary paragraph) and RSLT-03 (aggregate score)

5. Create `src/components/results/ScoreOverview.tsx`:
   - Props: `{ categories: CategoryScore[] }`
   - Section heading "Score Breakdown"
   - Maps over categories rendering a ScoreBar for each
   - Wrapped in a space-y-3 div
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>score-utils.ts exports 3 functions, ScoreBar renders a color-coded horizontal bar, ResultsSummary shows aggregate score + summary text in a Card, ScoreOverview maps categories to ScoreBars, Collapsible component installed</done>
</task>

<task type="auto">
  <name>Task 2: Build CategoryFeedback and compose ResultsPage with empty state</name>
  <files>src/components/results/CategoryFeedback.tsx, src/pages/ResultsPage.tsx</files>
  <action>
1. Create `src/components/results/CategoryFeedback.tsx`:
   - Props: `{ category: CategoryScore }`
   - Uses shadcn Collapsible wrapping a shadcn Card
   - CollapsibleTrigger wraps CardHeader (cursor-pointer, select-none)
   - Header shows: category name (CardTitle), score fraction, ChevronDown icon that rotates 180deg when open (use cn() with "rotate-180" conditional)
   - State: `const [open, setOpen] = useState(false)` — default collapsed
   - CollapsibleContent contains CardContent with three subsections:
     - "Strengths" with a green-tinted icon or heading, bulleted list of category.strengths[]
     - "Areas for Improvement" with an amber-tinted heading, bulleted list of category.improvements[]
     - "Justification" with the justification text as a paragraph
   - Use space-y-4 between subsections, space-y-1 between bullets
   - Each bullet as a flex with a small dot/dash and text-sm

2. Rewrite `src/pages/ResultsPage.tsx` (replace entire skeleton):
   - Import useAppStore, useParams from react-router, Link, Button
   - Import ResultsSummary, ScoreOverview, CategoryFeedback
   - Read `currentResult` from store via `useAppStore((s) => s.currentResult)`
   - Empty state guard: if `!currentResult`, show centered div with "No grading result found." message and a Button linking to "/grade" ("Grade an Essay")
   - When result exists, render:
     - h1 "Grading Results" (text-2xl font-bold)
     - ResultsSummary with result prop
     - ScoreOverview with categories prop
     - "Detailed Feedback" h2 section heading
     - Map currentResult.categories to CategoryFeedback components (key={cat.name})
   - Use space-y-6 for overall layout
  </action>
  <verify>
    <automated>cd /Users/matthewgardner/essay-grader && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>CategoryFeedback shows collapsible card with strengths/improvements/justification. ResultsPage composes all components reading from Zustand store. Empty state shows when no result. Full build succeeds with no type errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. `npm run build` succeeds
3. Manual check: navigate through submission flow (enter essay text, submit) and verify results page shows summary, score bars, aggregate score, and expandable feedback sections
</verification>

<success_criteria>
- Overall summary paragraph visible at top of results page (RSLT-01)
- Per-category score bars render with green/yellow/red color coding based on score percentage (RSLT-02)
- Aggregate total score displayed prominently with colored text (RSLT-03)
- Each category has an expand/collapse section showing strengths, improvements, and justification (RSLT-04)
- Empty state shown when navigating directly to /results/:id without store data
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-results-display/04-01-SUMMARY.md`
</output>
