# Dataset generation
anthropic>=0.45.0
pandas>=2.0.0
kaggle>=1.6.0

# Fine-tuning (install on training machine with GPU)
# pip install unsloth
# pip install --no-deps trl peft accelerate bitsandbytes

# Evaluation
scikit-learn>=1.3.0
numpy>=1.24.0
